

var name = 'ROGER';

console.log(name);


/**
 *  var   = not recommended to keep using
 *  let   = common word to use and declare variables (eventually change)
 *  const = variable with a value that not change (never change)
 * 
 * scope  = space of memory of a specific code (limits) 
 */