

var name = 'ROGER';

console.log(name);


/**
 *  var   = not recommended to keep using (global variables)
 * 
 *  let   = common word to use and declare variables (eventually change, scope variables)
 * 
 *  const = variable with a value that not change (never change , scope variables)
 * 
 * scope  = space of memory of a specific code (limits) 
 */